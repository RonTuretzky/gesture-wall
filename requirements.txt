# Core runtime (needed for the app, including camera-free mouse mode)
numpy>=1.24
opencv-python>=4.8

# Pose tracking — required only for `--source pose`.
# Verified working on Python 3.13 / Apple Silicon with mediapipe 0.10.35.
# If your Python version has no mediapipe wheel, the mouse mode + tests still run.
mediapipe>=0.10.9

# Dev/test only:
#   pip install pytest
